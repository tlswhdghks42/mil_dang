{
  "installCommand": "npm ci",
  "buildCommand": "cd mobile && npm ci --legacy-peer-deps && npm run build:web",
  "outputDirectory": "mobile/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
